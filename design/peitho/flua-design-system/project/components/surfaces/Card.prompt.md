Surface + structural components.

```jsx
<Card padding={20} interactive>…</Card>
<Tabs items={[{value:"all",label:"Todos",count:128},{value:"mine",label:"Meus"}]} />
<Tooltip label="Ligar" side="top"><IconButton icon="phone" /></Tooltip>
```

`Card` — bordered surface, soft shadow; `interactive` lifts on hover. `Tabs` — underline style, accent indicator. `Tooltip` — dark caption on hover, four sides.
